# Codex / Astra視点での継続レビュー・実装記録

2026-09-21。利用者の指示に従いサブエージェントを使わず実装・レビュー・リリースを行う。
元の `OJT` 作業ツリーは未コミット変更を保全し、`origin/main` から作った専用 worktree だけで作業する。
`docs/reviews/**` と v1.0.0 のリリース記録は変更しない。

## B+C レビュー修正

Blocking 0件 / Important 3件 / Minor 4件を修正。既に解消していた2件は再確認。

| 所見 | 対応・確認 |
|---|---|
| I1 編集エラーに内部IDが出る | 行・列・ブロック編集の全診断を回復方法のある日本語へ変換。ツールバー経路も共通関数へ。実際に20行を超える操作と9種類の診断をテスト |
| I2 未参照翻訳 | Task 22で解消済み。`i18n-keys` で再確認 |
| I3 ENDの前に置いた回路が無視される | ENDのあるブロックを事前に検出し、前後どちらのセルも拒否。ランタイムの実行単位に合わせた |
| I4 課題数制限の迂回・全件消失 | 共通ローダーに件数予算を渡す。直下と1階層下のJSONを合算し、200件を読み込み、超過だけ警告。メモやフォルダを件数に含めない |
| M1 コミットの混在 | 履歴の事実として保持。既存コミットを書き換えない |
| M2 報告欄の案内スタイル欠落 | `.hint` を追加 |
| M3 タグ絞り込みの説明 | Task 25の実装済み機能と一致しているため変更不要 |
| M4 タイマの刻みを重複定義 | 全方言が `timerBaseMs` を規則から公開。入力欄の100ms決め打ちを削除 |
| M5 乱数seedの保存 | 意図的な逸脱として承認。作業ファイルは解決済みfaultsの完全な一覧を保存するため、復元時に再抽選しない。seedだけを追加するより元の故障を確実に再現できる |
| M6 テスターのレンジ識別 | `stepTester` の実呼出しで種別・Ωレンジの識別をテスト |
| M7 重い判定テストの時間上限 | 2ファイルに限って120秒。通常テストの既定値は維持 |

## D+E レビュー修正

- ラダー入力の成功後・取消後は編集面へフォーカスを戻す。無効な命令は入力欄を維持し、カーソルを進めない。入力中のモード変更も再検証する。
- 入力欄の Enter / Escape が編集面へ伝わり、別操作を起こす経路を防いだ。
- 未使用の旧 `TerminalHit` コンポーネントを除き、端子の当たり判定・強調表示を実際の `TerminalField` でテストする。
- 視点キューブの仕様を実測に修正。上面から正面は上方向ドラッグ。極限角度では一部方向がクランプされることを明記した。
- 既知のE2E失敗4件は、目次の外枠を章に数える、検索の抜粋を見出しと誤認する、入力後の自動移動に余分な矢印を加えるテスト側の不一致だった。実装の意味に合わせて修正。
- README / CONTRIBUTING で通常E2Eと手動の画像更新を区別。利用者の変更を破棄せず、配布時はクリーンな専用worktreeを使う。
- 再レビュー: 配線経路キャッシュは不変配列参照に追随、ライブ波形は最新2000点を保持、空タグ2課題は正当、モニタ操作はメーカー別のモード定義と一致。追加修正不要。
- 検証: 関連単体8ファイル173件、追加後のラダー編集48件、型検査、ヘルプとPLC4社のE2E20件が成功。

## Task 23: 色・状態の伝え方

- 用紙とアプリの配色を共通トークンへ整理。回路図2スタイルシートの直書きhexは0件。
- 通電／無通電／保護動作を文字と形で識別。線色ボタンは色見本を併記。
- 期待波形と凡例を破線に変更。既存のラダー通電枠は4スキンの描画テストで確認。
- 8箇所の固定インラインstyleをクラスへ移し、回路図viewBoxの数値→文字列→数値の往復を除去。
- 追加レビュー: chart / polish E2Eの級フィルタを対象課題に合わせた（b-007は1級）。
- 検証: 5ファイル47件とラダー・電源表示42件、型検査、変更ファイルlint成功。全画面品質はTask 24/26の変更後にも再計測する。

## Task 24: アクセシビリティと画面の一貫性

- 共通の折りたたみパネル、端子一覧・ライブ記録の初期折りたたみ、12pxの3D名札と重なり制御を導入。3級の回路図は常時表示を維持する。
- メニューは項目選択・外側クリック・Escapeで閉じ、広い画面では保存・読込・視点を直接表示。判定中の進捗、タイマーの残り時間、キー早見表を整備。
- 設定の無変更保存を抑止し、課題フォルダの検証をmainと共有。音声の再開、指摘ポップオーバーのフォーカスも修正。
- C1回答欄は狭い領域では部品別カード、広い領域では部品×原因の表。サイズ変更後も選択を保持するテストを追加。
- Astra視点の追加レビュー: Blocking 0 / Important 2 / Minor 1。画面遷移時のCanvas終了を障害として次画面へ持ち越す問題、折り返したツールバーとヘルプの重なり、通知の重複を修正。実際のWebGL障害は回復まで表示し、再表示ボタンを4画面に共通化。
- 検証: 関連10ファイル146件、追加・再検査6ファイル115件、desktop型検査・変更箇所lint成功。ヘルプ/C1/C2 E2E17件成功、回路図/仕上げ8件成功。全画面監査の最終しきい値はTask 26後に実測する。
- 画面確認: 1280×800のホーム・盤・ヘルプ・PLCを撮影。広い画面も含むヘルプの横はみ出し0。PLCの編集面の狭さは追加のメーカー別再設計で改善する。
- 監査手順も修正: OMRONのコメント確定、課題級フィルタ、折りたたんだ端子一覧、通電危険の測定箇所を実操作へ合わせた。基準値を上げて失敗を隠していない。

## Task 24後のCI不整合と再発防止

- 利用者報告の10件を修正。メニュー項目を押すと閉じる仕様に対し、破棄されたDOMを再クリックするテストを実操作へ修正。設定リセットは変更済み／変更なしの双方を検証する。
- 回復方法を伝えるラダーエラー、3分類の視点早見表へ期待値を同期。説明書の機能一覧・WebGL復旧案内・生成済みヘルプを更新した。チェックの削除や基準値引上げは行っていない。
- 物的対策: 追跡するpre-pushフックをinstall時に設定。送信する全差分から関連テストを自動選択し、説明書との整合6項目は常時必須。未コミット・未追跡コード、別コミットの送信、検査失敗はpushを止める。
- 関連9ファイル214件、型検査、変更箇所lintが成功。ゲート自身の7テストでは、実際のGit送信を拒否して送信先のコミットが変わらないことまで検証。CIにも追加。今後のコミットメッセージは分かりやすい日本語に統一する。

- フック実行時にGitの環境変数が検証用リポジトリへ漏れる問題を検出し、子プロセスのGit環境を分離。最初の専用作業ブランチに混入した検証用コミットは送信せず、正常な修正コミットから別worktreeを作成して続行した。元の共有作業ツリーと公開履歴は変更していない。

## Task 26: 文字サイズ・高コントラストと見やすさ

- 90 / 100 / 115 / 130%の表示倍率と高コントラストを設定・保存。ヘルプと3D名札にも即座に適用。高コントラストの本文・補助文字は実際のCSS色で4.5:1以上を検査する。
- ホームは4つの学習目的を図と短い見出しで選べる構成へ刷新。ヘルプは明るい用紙、読みやすい行幅、目次と手順の強弱を整えた。
- PLCの初期ウォッチを閉じ、操作列を小さく整理。狭い画面でも編集面を確保し、閉じたパネルの見えない子要素を監査が誤検出しないようにした。
- 物的対策: 編集領域240×180px未満をE2Eで拒否。名札が遅れて描かれた場合も重なりを除去し、文字倍率の変更にも追随する。ボタンの改行は文字の実測で判定する。
- 検証: 関連12ファイル262件、型検査、変更箇所lint、画面品質E2E12件成功。1280×800・1440×900・1920×1080と130%表示を検査し、はみ出し・文字切れ・重なり・不自然な改行・歩けない画面は0件。
- 送信前ゲートが端子図の旧11px固定テストを検出してpushを拒否。12px以上・倍率追随の現仕様へ照合を直してから送信した。
- 基準値を実測まで引き下げた。縮小SVG内の小文字72件とチェック欄の小さい当たり判定89件は残課題として追加UIレビューで扱う。これらを0件と報告しない。

## 最新依頼を反映する方針

- 追加指示: 以降のコミットは、変更内容と目的が分かる日本語のメッセージにする。
- ボタンや語の途中の不自然な改行、過大・過密な操作部を実画面で検査する。PLCは各社の純正ソフトの構成・操作を優先する。

- 既存41タスクを完了したうえで、機能の発見しやすさと見た目を実画面で再評価する。
- 3D盤では測定する場所とプローブの選択・測定値を結び付け、次に何を押せるかを明示する。
- ヘルプとPDFは情報の強弱、余白、見出し、手順、図を一貫して設計する。撮影は全実装後。
- PLCは公式公開資料を再調査し、4社の枠組み・入力・キー操作を個別に検証する。確認できていない仕様を確認済みとしない。
- ポータブル版は単一EXE配布を優先。実行時の一時展開と、利用者が受け取るファイル数を区別して説明する。
- 最終 `verify`、E2E連続実行、配布物検査、実アプリ操作が完了してから v1.1.0 を公開する。

進捗: 34 / 41着地（既存29 + Task 23・24・26・28・31）。レビュー修正は元のタスクを再計上しない。

2026-09-22に引継ぎ書の全41行と各コミットのorigin/mainへの包含を照合。引継ぎ時点の着地一覧は29件で、本文の「28件」が集計誤りだった。履歴を根拠に合計を1件訂正した。訂正時の残りは31・35・36・37・38・39・40・41の8件。


## メーカー純正画面の追加レビュー（31 / 41のまま）

- 公式PDFを図と操作手順まで確認。メーカー別のメニュー順、文書帯、左ツリー、OMRONの紫カーソル・黄色ラング、SHARPの水色選択と赤い枠を反映。常設コマンドを6個以内に整理した。
- SHARPにS/D/X→未入力記号の配置→Enterでアドレス入力を実装。作業保存・取消・やり直しで記号を保ち、未入力記号はコンパイルで必ず拒否する。
- OMRON ClassicのX（OR b接点）・Ctrl+矢印（作図）・F2・Ctrl+M・Ctrl+Tへ修正。JTEKTに三菱キーを流用しない。確認範囲と未実装の固有機能は出典表に記録した。
- 回路一覧・ステータス・エラー欄から内部ネットワークIDを除去。先頭回路を削除した後のカーソルと、メニューの編集禁止チェックも修正した。
- 再発防止: 公式キー・メニュー順・記号先行入力・未完成回路の実行拒否を30件の画面テストと11件のコアテストで検査。4社入力・3D端子・レイアウト12件、画面品質4件のE2Eが成功。関連テストの旧仕様前提を訂正し、送信前ゲートを実行する。
- ツリーの見出しとプログラム名は短い固有名を表示し、文字倍率に応じて幅を確保。全名称は文書帯とツールチップで読める。文字切れ42件を解消し、4社・3画面サイズと130%表示のE2E5件が成功。部分実行でも文字切れ・重なり等があればそのテスト自体が失敗するようにした。

## Git名義の事故と対策

旧pushゲートのテストがGitの環境変数を親から継承し、共有リポジトリの名義を `Gate test <gate@example.invalid>` に変更した。環境分離の修正後に名義復旧を漏らし、公開済み5件がこの名義になった。認証アカウントは変更されていない。2026-09-21にローカルの誤設定を除去して従来のyuyaへ復旧した。

84d7867で、テストの名義をプロセス内の `git -c` に限定し、pre-commit / pre-pushでテスト用名義を拒否する物的対策を追加。実際のcommit/pushの拒否、親の設定・HEAD・インデックス不変を含む10件が成功。公開済みの履歴は書き換えていない。

## Task 28: 初回の3分操作ガイド

- 初回の回路組立で、回転・装着・配線・通電・判定の実操作に追随する5段階の案内を追加。対象部分を明るくし、操作対象と案内の間をキーボードでも巡回できる。
- あとで／次回から表示しない／Escapeの終了状態を保存。設定とF1ヘルプから再開できる。WebGLの準備前や表示中断中は操作を覆わない。
- 盤コマンドの直接更新を値の控えで検知する。実装と同じ更新経路をテストに使い、装着しても進まない問題の再発を防いだ。
- 旧3D操作E2Eの独自起動を共通の一時userData起動へ移行。specからの直接Electron起動をlintで拒否し、利用者の設定をテストが使わないようにした。
- 検証: ガイド・設定保存・説明書整合など85件、既存3D直接操作E2E5件、ガイドの一周・再起動・再表示・130%表示E2E2件が成功。型検査と変更箇所lintも成功。図の最終撮り直しはTask 36で行う。


## Task 31: 単一EXE配布と実物を検査するゲート

- ポータブル版を1個のEXEに集約。起動ごとの一時展開と終了時の消去を実際の配布EXEで確認。設定・一時保存は従来の利用者フォルダを使う。
- 旧版混入、課題の欠落・内容違い・余分なモード、asarの必須ファイル・版数・不要依存、PDF形式、アイコン、実行ファイルに焼いたfuseを検査。失敗時は古い成果物一覧も残さない。
- 検査関数とCLIを分離し、異常な配布ツリーを作る20テストで拒否を確認。複写時は対象範囲を検証し、ジャンクションのリンク先を削除しない。
- 説明書の日付を指定日→コミット日→Gitがない場合だけ当日へ変更。ワークスペース依存をpackage.jsonから自動抽出する。
- 検証: 関連188件、型検査・lint成功。02182baからdistを2回実行しPDFのSHA256一致（1A1BAFA2…FD20D82B）。PDF71ページ・リンク97・しおり107。代表ページの表示を確認した。
- 専用の配布版E2EはEXE1個を別フォルダへコピーし、一時userDataで全72課題、3D通電・判定、F1ヘルプ、PLC画面、終了時の展開先削除まで成功。終了直後のEXEハンドル保持に対し、close待ちと非同期削除に修正した。
- 最終版の説明書デザイン・画面画像はTask36で更新し、v1.1.0への版上げと公開はTask38で行う。


## Task 39: 採点と同じ動きの見直し

- B・C2・Dの結果から区間を1歩ずつ再生。操作列の実行器を採点と共用し、差分マークは採点結果そのものを使用する。C2の解決済み故障は抽選し直さない。
- 同時操作は同じtickでまとめる。操作前の差分を落とさないため最初の待ち時間も1区間とした。前へ・最初からで区間末の保存フレームを読み直す。
- 編集漏れを構造的に防ぐため、通常セッションではなく編集ハンドラを持たないReplayScreenを使用。intentOfとWorkerでも編集を拒否する。Workerは元のSimulationを保持したまま別の実行器を使い、終了後の時刻・スナップショット・履歴・所要時間・危険操作を保つ。
- 見直しの操作説明と網羅表を同時に更新。採点との一致（B/C2/D）、時刻境界、同時操作、戻る操作、Workerの編集拒否と復帰、実画面の往復を検証した。


## Task 40: 模範と自分の比較

- 模範は破線・自分は実線で、信号ごとに同じ時間軸へ並べた。拡大・案内線は既存の図と共用。▲一覧は時刻・部品名・理由を表示する。
- 波形の変化点をSignalLogへ戻し、採点のcompareLogsと同じ許容差で比較。B/C2/Dの実判定と差分一覧が完全一致するテストを追加した。帯は値が違い、かつ許容されなかった変化を含む区間だけに限定する。一致へ戻った後と許容済みの別のずれを誤って塗る回帰を防ぐ。
- B3級・C2の2級かつ公開フラグが立つ課題だけ模範回路図と配線の差を表示。差は接点割当と節点分割から求め、既存の5件上限と残り件数表示を共用する。PLCでは模範ラダーを表示しない。
- 比較と拡大が重なった際のEsc・フォーカス復帰を実画面で検証。1280×800の1級・3級でE2E2件成功。型・lint・説明書整合も検査した。
- 追加レビュー: 見直し終了後に作業へ戻った際も、見直しに使った時間を所要時間へ加算しないようにした。60秒の見直しを挟む回帰テストで確認。


## Task 41: 今回の結果を1枚へ書き出す

- 4モード共通の「この結果を書き出す」を追加。OSのドキュメントから保存先を選び、PDFか単独HTMLへ1回だけ保存する。キャンセルは無言で、アプリに成績・前回の保存先・書き出し履歴を残さない。
- IPCを仕様どおり9本へ更新。1 MiB・型・安全な既定名・拡張子・同時実行をmainで検査する。印刷窓は非表示、JavaScriptなし、sandbox有効、preloadなし、非永続の専用セッション。権限と外部通信を拒否し、30秒で打ち切り、成功・失敗とも一時フォルダと窓を消す。HTMLの先頭にも制限付きCSPを付ける。
- 説明書と紙の配色を共用。静的チェック・表示名での動作差5件・配線候補3件と総件数・危険操作とヒントの回数をまとめる。C1は正解数、C2は指摘数、PLCは変換エラー数も扱う。利用者名の欄を持たず、内部の線ID・絶対パスを出さず、自由文はエスケープする。
- 開始時刻は所要時間の原点と分けて保持。「開始（今回）」は課題を開いた日時で、復元・動きの見直しに影響されない。この意味と一覧の要約範囲を説明書へ記載した。
- 検証: 型・lint、関連12ファイル216テスト、4モードのPDF/HTML保存・取消・設定不変・窓破棄の実画面検査が成功。長い課題名と大量の配線候補で2ページになる回帰を修正し、同条件でも1ページを保証するE2Eを追加。PDFの日本語と配置を画像化して確認した。
- push前の129ファイル検査で説明書側に残る旧IPC本数の固定を検出し、公開前に修正。正確な本数・重複・登録漏れは共通の公開面テストへ集約し、機能別テストは既存8チャネルの維持を検査する。新しいチャネル名も共通側で固定した。

## F+G 最終UIレビュー

- Important 7件・Minor 5件を発見し修正。詳細は `2026-09-22-final-ui-review.md`。
- 全4モードの3D電源、PLC部品の運搬、中断後の操作復帰、中ボタンの視点移動、重ねた窓の解除を修正。ラダーの編集幅を拡大し、SVG文字と操作領域の最小寸法を確保した。
- 画面監査12件＋直接操作9件が成功。全4メーカー・3解像度と130%高コントラストを含み、監査指摘は0件。小文字・小操作領域の許容件数を0に固定した。
- 型・lint・関連単体328件・説明書整合が成功。未定義CSS変数の検査をpush前の必須7ファイルへ追加した。
- 現在地は37 / 41。残りはTask35（通し練習・機能別説明）、36（図とPDF）、37（全体検証）、38（リリース）。

## Task 35: 通し練習と機能別の説明

- 4モードを9・9・10・12手順で案内し、各手順に図の参照を付けた。Bの自己保持と三菱PLCの入出力には端子ごとの配線表を追加。b-001にタイマはないため、手順5は要否の確認とb-003への案内にした。
- 15機能を「何ができるか・どこにあるか・操作の順・よくある間違い」で統一。最終節に実課題と同じ72件の索引を作成し、IDの重複・欠落とモード・級・難・題名を検査する。
- 新しい図23枚の意味を定義（合計40枚）。吹き出しの場所と実画像はTask36で一括撮影する。操作・キー・ジェスチャーの網羅表を新章に結び付け、全22節を番号付き手順の検査へ追加した。
- 原稿の実操作確認で、結果から作業を保ったまま戻る導線が4モード共通になっていないことを発見。「作業へ戻る」を共通の操作バーに追加し、「もう一度」は盤を作り直すことを明記。4モードで、戻る前後に保存した作業内容の一致・電源操作・再判定まで実画面テストが成功した。
- 図を挟むと印刷の手順番号が1へ戻る問題も修正。Markdownの開始番号を印刷用の丸数字へ引き継ぎ、ヘルプとPDFの両方を検査する。
- 現在地は38 / 41。残りはTask36・37・38。
