You are implementing Plan 2A Task 15 (built-in C2 problems, first four: 2級 form with schematic) of `docs/superpowers/plans/2026-09-14-phase2a-fault-content-and-judge.md` in `C:\Users\oltot\Documents\git-projects\OJT` (pnpm monorepo; TypeScript strict; Vitest; ESLint; Prettier width 100 — note `packages/content/builtin/*.json` ARE formatted by Prettier; JSON imports use `with { type: 'json' }`). Read verbatim first: `## Task 15` (L6416–6987), `## 前提` (L15–49), `## 仕様からの意図的な差分` (L8240–8261), and spec §17 (`docs/superpowers/specs/2026-09-13-ojt-electrical-trainer-design.md`) for the built-in problem requirements. Task 15 only needs `faults.ts`, `inspect-repair.ts`, `random-faults.ts`, `reference.ts`, `runner.ts`, `timechart.ts` (all landed) — it does NOT need the judge; Task 16's 弁別 tests come later. Write the four JSON files exactly as the plan gives them, then run the plan's tests (schema parse via `parseProblem`, `buildInspectRepairCircuit` succeeds for each, fault sites resolve, reference time chart matches the spec chart, B-6: per-problem `it()` blocks and `testTimeout` as the plan states). If a plan JSON fails its own test (e.g. a schematic that does not reach P/N, or a fault site whose wire id does not exist on the JIPM board), fix the JSON minimally, keep the intended fault, and list the fix. Files you may touch: the four new `packages/content/builtin/c2-*.json` files the plan names, `packages/content/test/builtin-c2-a.test.ts` (or the plan's test file name), and `packages/content/test/helpers/*` only if the plan says so. Do NOT touch `builtin/index.ts` (Task 14 registers everything) or `src/index.ts`. Other agents are editing `src/judge-inspect.ts`, `src/judge.ts`, `apps/desktop/**`, `packages/circuit-sim/src/tester.ts` concurrently — never touch those; never `git add -A`; stage exact paths; `git commit --only -- <paths>`; retry on `index.lock`. Verification scoped to the package (`pnpm --filter @ojt/content test`, typecheck, eslint on your test file, `pnpm exec prettier --write` on your files). Report (≤ 15 lines): SHA, the four problem ids/titles/fault kinds, test counts, JSON fixes if any.
