# Deferred findings (fold into the named batch/task brief)
## From 2A A+B review (a87f4665692037a16, 21:41)
- Task 17 (Batch G): barrel must export SupportedProblem, isAssembleProblem, isInspectPartsProblem, isInspectRepairProblem; swap the inline predicate in apps/desktop/src/main/content-loader.ts for the import.
- Task 17 (Batch G) tests: isRandomFaults() true/false (schema/faults.ts:207 untested); ofMode(parseBuiltinProblems([inspectPartsProblemJson()]), isAssembleProblem, 'モードB課題') throws BuiltinProblemError (builtin/index.ts:70); mergeProblemSets with user C2 overriding a built-in C2 + user C1 appended (loader.test.ts has zero non-assemble cases).
- Task 17 or 2A polish: schema/index.ts:138-142,177-182 typo'd mode ('inspect-part') yields 8 assemble-schema issues — emit one issue naming the valid modes when mode is an unknown string.
- Later (2A polish): reversed over-range needle (−24 V on 2.5 V) reports OL but targetDeg=0 — decide and document (§9.3 says 振り切れ位置). analog CONT `value` carries +5 % but `conductive` uses raw ohms (near 50 Ω disagree). place-probe with unknown probe name moves the red probe silently. C2 schema could cross-check faults[].target.partId against board.socketRoles (catch coil-layer-short on timer, partId 'CR9'). random.fallback[].kind ⊆ types not enforced — document intent in JSDoc. C1 inventory non-empty accepted but ignored (#14). sim.reset() vs rangeExceededReported asymmetry (fixed if WeakMap fix lands). readTester does not check probe terminals exist (unknown id reads as ground). DCV 2.5→ACV→DCV lands on 10 V (per-mode range not remembered).
- Docs: plan 2A 完了条件 tester.test.ts 13件 → 14 (now more after fixes).
## From 2B review (a5044d7bf8bf8f98b) — all sent to docs agent a358dcebb031bcf48
## For the next Opus review (Batch E+F, or 2A final): re-verify circuit-sim tester fixes e5c9237 (Ω WeakMap cache keyed tMs+probes+kind; NEEDLE_SNAP_DEG=0.05) and 2bb24d3 (range-exceeded WeakMap record with tMs-rewind reset check; never-exhaustive fallback; set-ohm-range ignore; dtMs clamp). Probe: 10× stepTester discarding state → 1 hazard; sim.reset() then re-excursion → hazard re-armed; cache hit does not suppress a NEW ohm-on-live after probe move; NEEDLE_SNAP_DEG not exported (plan export list omits it — decide whether 2B needs it).
## Review plan: ONE Opus review for D2+E+F (Tasks 9,10,12,14,15,16: b54c927 48254b8 36e57aa ebeb9cb 32025e0 + Task 14/16 SHAs) + tester fixes e5c9237/2bb24d3 re-verification, dispatched after Tasks 14/16 land (in parallel with Task 17). Then Task 17 → 2A acceptance (§14.3) rolled into the 2B/Phase 2 acceptance on the packaged app.
## C+D1 review (a41e29238e9e81544, 22:24): CHANGES REQUIRED → fix agent dispatched (B1 forbidden.ts load→link; I2 M-15 test restore; I1 misroute destination validation; I3 resolveFaults clone + maxMillis; I4 fallback validation + fellBack; M1–M6). Missing tests 7 (preexistingWireIds empty set) and 8 (highlight staleness) → fold into Task 17 or the 2A final review fix batch.
## C+D1 fix batch result (22:40 shutdown): items 1 (715f915: forbidden.ts link-only + M-15 restored + b-005 regression + 464-board sweep) and 2 (8635cab: misroute destination validated via exported `checkWirableTerminal()`/`toSessionTerminal()` from @ojt/board-model; `applyFaults(session, faults, board = loadBoard(session.boardId))`; self-loop rejected; M1 JSDoc) DONE. content 32 files/409 tests, board-model 155.
## STILL TO DO (next session, one Opus/Sonnet task): item 3 random-faults.ts — clone the reference session in `isUsable` instead of `buildReferenceSession` per attempt (measure ms/attempt on b-001 @ durationMs 2000), validate `spec.fallback` (ok:false at `faults.random.fallback` if unusable), add `fellBack: boolean` to the success value (+ `fellBack: false` in inspect-repair.ts synthetic result), add `maxMillis?: number` (default 5000) to ResolveFaultsOptions; item 4 M3 duplicate faults on one wire rejected (+tests), M4 `SOCKET_PIN_COUNT` replaces hardcoded 14 at random-faults.ts:84, M5 fix over-strong c8 comment at :131, M6 comment on part-fault under-draw; item 5 plan doc: handoff M-11 note (applyFaults replaces session.wires), resolveFaults row (fellBack, maxMillis, 36 s worst case), 改訂履歴 row 「C+D1 レビュー反映」. Reviewer note: `CHK.1`/`T2.1` are accepted by addWire when the role is assigned (not crashes); only unknown pins like CR1.99 crash routing.
## 2026-09-17 00:30 — C+D1 fix items 3–5 NOT started (agent stopped read-only at shutdown). Investigation notes to reuse:
- Clone safety for item 3(a) CONFIRMED: `applyFaults` (faults.ts:121-204) only replaces `session.wires` (draft = wires.map(w=>({...w}))); `checkWirableTerminal`/`toSessionTerminal` are read-only; `toNetlist` (board-model to-netlist.ts:106-141) copies wires → `{ ...session, wires: session.wires.map(w => ({ ...w })) }` is sufficient.
- M4: `SOCKET_PIN_COUNT = 14` at board-model/src/board-jipm.ts:37, re-exported index.ts:55 → swap at content/src/random-faults.ts:84.
- Item 5 plan lines: M-11 note L8306 (buildHighlightIndex row); `resolveFaults` row ≈ L8335 (共通 table); 改訂履歴 starts L8340 (append after L8346).
- Item 3(c): inspect-repair.ts:71 `{ ok: true, value: [...options.resolvedFaults] }` → add `fellBack: false`.
- Benchmark: use C2 built-in `c2-001-self-hold.json` (resolveFaults needs an InspectRepairProblem; b-001 is mode B); durationMs 2000, operations that make every attempt fail `isUsable`, `maxAttempts: 100`, wall/100. Runner: `node --experimental-transform-types --import packages/content/scripts/ts-source-resolve.js <scratch>.ts`.
- Baseline: random-faults.test.ts 14 `it`; content 32 files/409 tests.
## 2026-09-17 00:35 — Plan 2A Task 17 PARTIAL (b12caed): barrel `src/index.ts` now re-exports the whole Phase 2A surface (every 2B handoff-table name present, incl. BUILTIN_ALL_PROBLEMS, SupportedProblem, isAssembleProblem/isInspectPartsProblem/isInspectRepairProblem, SchematicProblem, countHazards, judgeInspect*); `schema/index.ts` `peekRawMode()` → a typo'd `mode` yields ONE schema issue at path `mode` naming the valid modes; desktop `content-loader.ts` now imports `isAssembleProblem`. content 409 tests + `pnpm -r typecheck` green. STILL OPEN for Task 17: barrel smoke tests (`test/index.test.ts` Step 2), `apps/desktop/test/content-loader.test.ts` check, the deferred tests (isRandomFaults, ofMode throws, mergeProblemSets C1/C2, preexistingWireIds empty set, highlight staleness, typo'd-mode), whole-repo verification (`pnpm -r test`, lint, prettier --check, coverage ×2, `schema:write` diff, desktop build), plan checkboxes/改訂履歴. → 2B Tasks 1/4 are now UNBLOCKED (barrel exports exist).
## 2026-09-17 00:37 — Task 17 Step 2 landed 4689d7c (barrel smoke tests 24 + deferred tests; `ofMode` exported for tests; content 415 tests before the concurrent random-faults edits). STILL OPEN for Task 17: whole-repo verification (`pnpm -r test`, typecheck, lint, prettier --check, coverage ×2, `schema:write` diff, desktop build) and plan checkboxes/改訂履歴 — run after the C+D1 fix items land.
## 2026-09-17 00:36 — C+D1 fix items 3–5 DONE: 0257032 (isUsable clones the one reference session; fallback validated → ok:false at faults.random.fallback; `fellBack` on success; `maxMillis` default `MAX_RANDOM_FAULT_MILLIS=5000`; benchmark 44 ms/attempt before & after — the cost is the runOperations replay, not buildReferenceSession), 0b4c14a (M3 duplicate-wire faults rejected, M4 SOCKET_PIN_COUNT, M6 comment; M5 comment rewritten in 0257032), c024061 (plan handoff rows + 改訂履歴). content 32 files/429 tests, coverage 98.5/96.35/100/99.75. Open: no test for the applyFaults-rejects-a-drawn-candidate path (M5). Pushed. Task 17 whole-repo verification agent running (docs-only commit if green).
## 2026-09-17 00:45 — Plan 2A Task 17 COMPLETE: 5c0461a (barrel formatted), 155ca16 (plan checkboxes + 改訂履歴). Whole-repo verification at c024061: 102 files/1331 tests, typecheck, lint, prettier, content coverage 98.5/96.35/100/99.75, schema:write no diff, desktop build (circuit-sim coverage not re-measured; 98.9 % at 2bb24d3). ALL 17 Plan 2A tasks landed; pushed. Opus review of the second half (D2+E+F+17+fixes) running — its fixes are the last thing before shutdown.
## 2026-09-17 01:16 — Opus review of 2A second half (D2+E+F+17+fixes): APPROVED WITH MINOR ISSUES. All plan code verbatim; C1 all 7 truths discriminable by the §9.1 procedure; all 8 C2 built-ins pass the full matrix (16/16 sites individually detectable, white-wire rule holds even with wireColorRule off, hazards counted once, resolvedFaults restore identical, 0 forbidden findings); barrel 143 exports; typo'd mode → 1 issue; tester fixes 5/6 pass. Important: I-1 range-exceeded not re-armed after Simulation.reset() (add clearRangeExceeded in meter-state.ts, call from resetInternal, drop tMs heuristic); I-2 DIAGNOSIS_TABLE lacks §9.1 溶着 precedence (a-weld vs b-open, b-weld vs a-open ambiguous for c1-002 p3, c1-003 p3, c1-004 p2/p3). Minor: M-1 last duplicate answer wins (document/test), M-2 ratio 0.85 boundary reads normal on un-adjusted analog, M-3 export RepairCircuitOptions, M-4 judgeInspectRepair 0.2–0.45 s → 2B must run it in the Worker; store.ts sessionForProblem needs SupportedProblem in 2B Task 1. Perf: buildInspectRepairCircuit 0.1–0.5 ms (random ~205 ms). Fix agent dispatched for I-1, I-2, M-1..M-3, missing tests 1–5, plan 完了条件 count.
## 2026-09-17 01:05 — 2A final review fixes landed & pushed: f487356 (range-exceeded record moved to meter-state.ts, cleared in resetInternal), fb09e13 (DiagnosisRow.note + exported `diagnoseCheckReading(reading, nominalOhms=COIL_OHMS)` applying 溶着 precedence; RepairCircuitOptions exported), cbb288a (last-duplicate-answer documented+tested; ratio note in schema + task.schema.json regenerated; C2 per-site 16-pair test; inspect-repair +2), f16717b (plan counts + 改訂履歴). Root 102 files/1345 tests; coverage circuit-sim 98.91/93.80/100/99.89, content 98.52/96.44/100/99.75; typecheck/lint/prettier green. PLAN 2A COMPLETE AND REVIEW-CONFIRMED. NEXT: Plan 2B Tasks 1→4 (Opus, serial; MERGE notes; store.ts sessionForProblem → SupportedProblem; judgeInspectRepair 0.2–0.45 s must run in the Worker), then batches 2–6; push after each batch.
## 2026-09-18 00:32 — Plan 2B Task 1 landed 3c6a8db (`feat(desktop): list all twenty builtin problems with their mode`): `shared/ipc.ts` `SessionMode`, `ProblemSummary.mode`, `readProblem → SupportedProblem | null`; `main/content-loader.ts` uses `BUILTIN_ALL_PROBLEMS` (20 built-ins + user C1/C2 listed; the 2A-era `unsupported-mode` rows removed); interim non-assemble guards in `renderer/screens/ProblemList.tsx` (Step 6a, toast `JA.problemList.loadFailed`) AND in `renderer/session/work-file.ts` (plan gap: restore path also calls `store.openProblem`; uses `workFileProblemMissingText`) — Task 4 Step 10 must remove BOTH guards. desktop 29 files/474 tests, root typecheck/lint/prettier clean; E2E/build not run. C1/C2 rows are listed but not startable yet. NEXT: Plan 2B Task 4 「ストアを3モードへ広げる」 (plan L1427–2176, 13 steps; start at Step 1 `apps/desktop/test/store-inspect.test.ts`; widen `sessionForProblem` to `SupportedProblem`; reset C1/C2 fields in resetSession/restartSession/abandonSession per B7), then ONE Opus review of Tasks 1–4, then Batch 2 (5+6 / 9 / 13 Sonnet parallel + 7 Opus).
## 2026-09-18 00:26 — NEW USER REQUIREMENT: 「タイムチャートはクリックで拡大したり縦の補助線入れて見やすくなるようにもしてね」 → Task CHART-UX (apps/desktop renderer chart component + CSS + ja.ts + tests; independent of the store): click any time chart → enlarged modal (Esc/×/backdrop closes; result screen shows 期待/実際 stacked on a shared axis); vertical guide lines = tick grid + dashed lines at operation edges spanning all rows + hover cursor line with time readout snapping to the nearest edge; cheap updates (no full re-render per mouse move); spec + plan docs sync; unit tests + one E2E screenshot; send before/after screenshots to the user. Dispatch first thing next session (Opus or Sonnet+review), in parallel with the Plan 2B Tasks 1–4 review.
## 2026-09-18 00:40 — Plan 2B Task 4 landed b00f512 (three-mode store; store-inspect 14 tests; desktop 30 files/488 tests at that commit; build green; pushed). Adaptations: one plan test rewritten (place-probe with undefined terminal = lift); ProblemList guard removed per Step 10 → C1/C2 rows now route to `session` and show 「課題が選ばれていません」 with no back button (DEAD END until Task 10 SessionRoute — to be judged by the Batch 1 review); work-file.ts non-assemble guard kept (→ Task 17); Result.tsx interim guard; resetSession delegates to openProblem (random-fault C2 redraws on retry — check I10); sessionForProblem widened to SupportedProblem. Batch 1 (Tasks 1–4) Opus review dispatched; Task CHART-UX (chart enlarge + guide lines) running in parallel.
## 2026-09-18 00:50 — Opus review of Plan 2B Batch 1 (Tasks 1–4): CHANGES REQUIRED. Tasks 1–3 verbatim; MERGE notes honoured; Worker decimation verified (OFF/one probe → 0 measurements; analog DCV → 25 measurements vs 27 snapshots per second; hazards exactly once). Findings: B1 BLOCKING C1/C2 rows dead-end at 「課題が選ばれていません」 with no back button AND the bridge still boots a Worker (fix in Session.tsx: `!isAssembleProblem(current)` guard in the bridge effect + a 一覧へ戻る button calling abandonSession; keep as a safety net after Task 10); B2 `snapshot.tester.kind/mode` stale while unmeasurable (carry live knob in the `!measurable` branch); B3 store `clearProbes()` clears `zeroAdjusted` but 2A/Worker keep it (remove from store, fix comments/tests); B4 `resetSession()` redraws C2 faults — must retry with the SAME faults on a FRESH board (rebuild via `buildInspectRepairCircuit(..., { resolvedFaults })`, keep `faultSeed`). Minor: restartSession fallback builds a fault-free board for C2 (route to abandonSession in Task 14); 40 ms measure vs 33 ms snapshot cadence is intended; `Result.tsx` interim guard wording. Batch 2 readiness: every store/protocol name Batch 2 needs exists; `setTester` (Task 17) and `SessionRoute` (Task 10) absent by design. Fix agent dispatched (B1–B4 + tests 1–8 + plan rows).
## 2026-09-18 00:50 — Task CHART-UX landed & pushed: 2aea5d3 (feat) + 53d378a (docs spec §7.7/§8.3 + plan-1d2 改訂履歴). New `panels/chart-scale.ts` (niceTickStep, tickPositions, nearestSnap, edgeTimes, operationEdgeTimes), `panels/TimeChartView.tsx` (EnlargeableChart, portal modal, ChartCursor child — no full re-render on pointer move), `timechart.module.css`; modal layer counter in `session/interaction.ts` (`pushModalLayer`/`isModalOpen`) consulted by `shouldIgnoreShortcut()`; result enlarged view stacks 期待/実際 on one axis with mismatch bands (mismatch highlighting newly added to small charts too). desktop 32 files/520 tests, E2E 10/10, typecheck/lint/prettier/build clean. Screenshots in scratchpad chart-ux/ (sent to the user). NOT YET REVIEWED — fold into the next Opus group review (2B Batch 2) or a short dedicated review; known nits: enlarged modal leaves a large blank band above the first row; touch-drag snapping untested on real hardware.
## 2026-09-18 01:00 — Opus review of Task CHART-UX: APPROVED WITH MINOR ISSUES. All requirements verified (open/close paths, focus return/trap, aria, no modal-layer leak on unmount/remount, shortcuts suppressed; tick count always 5–12, labels never overlap; zero waveform re-renders on pointer move; `timeReadout` byte-identical to old `formatMs`; live enlarged chart 0.1–2 ms/snapshot). Findings → fix agent: (1) enlarged modal wastes ~20 % height (LARGE_GEOMETRY rowHeight 58→74, amplitude 28→46, topPad 14→8; stacked 32→33/14→16; preserveAspectRatio xMidYMid); (2) dashed operation lines flood the small chart on b-007 (18 lines in 246 px → hide on small when > 10); (3) mismatch band contrast 1.21:1 → stroke + opacity .3; minor: z-index 50 ties with toasts (→40), 拡大 button covers plot corner, ChartCursor re-subscribes listeners every snapshot, per-wave durationMs vs shared axis, two-modal Esc/overflow, silent coarse snapping (731 ms at 30 s small) → chip tint + clamp, tickFont 8→9, 6 missing tests. Follow-up NOT in this fix: inert background + keyboard cursor stepping (←/→ between snaps) in the enlarged chart.
## 2026-09-18 01:03 — Plan 2B Batch 1 review fixes landed & pushed: 098d10e (Session.tsx: non-assemble guard in the bridge effect + 一覧へ戻る button; worker stays idle), ad21b19 (worker reports live tester kind/mode when unmeasurable; decimation contract pinned with spies), 63c9321 (zeroAdjusted kept on clearProbes), 4bbf54f (retry keeps the same faults on a fresh board: new `AppState.resolvedFaults` next to `faultSeed`; `openProblem(problem, { resolvedFaults, faultSeed })` now returns boolean; resolveFaults() called once in openProblem and fed to buildInspectRepairCircuit), b78dd54 (loader test for mode 'plc' → reason 'unsupported-mode'), 840e85f (plan: Tasks 1–4 ticked, 改訂履歴, Task 10 note). desktop 32 files/531 tests, root typecheck/lint clean, build OK. PLAN 2B BATCH 1 (Tasks 1–4) IS REVIEW-CONFIRMED. Note for Task 17 (work files): persist `resolvedFaults` from the store (2A `RepairCircuit` does not expose the resolved list in original order). NEXT: Batch 2 = Tasks 5+6 (tester panel digital + analog SVG) / 9 (C1 mark sheet) / 13 (C2 report panel) in parallel (Sonnet) + Task 7 (3D probes, warning banner, buzzer; Opus), then one Opus review.
## 2026-09-18 01:20 — CHART-UX review fixes landed & pushed: ec0aae7 (LARGE_GEOMETRY fills the modal, xMidYMid; `maxEdgeLines` 10 on the small chart; mismatch band stroke; backdrop z-index 40; `.frame` padding-top 18px; ChartCursor figure-in-ref; waves scaled by `figure.durationMs`, `ChartWave.durationMs` dropped; `pushModalLayer()` returns `{ depth, release }` + `topModalLayer()`; overflow lock counter; snapped-chip tint + tolerance clamp min(px, tickStep/4); tickFont 9), 4fd6e1c + 572f943 (tests; chart E2E now 2 scenarios incl. b-007 screenshot). desktop 541 unit tests, typecheck/lint clean, build OK, chart E2E passed. TASK CHART-UX IS REVIEW-CONFIRMED. Follow-ups: (a) inert background + keyboard cursor stepping (←/→ between snaps) in the enlarged chart; (b) the new b-007 chart E2E was flaky once on first run (residual result-screen state between scenarios) — isolate the scenarios (fresh app launch or explicit return to the list) when Task 18's E2E work happens; (c) full `pnpm --filter @ojt/desktop e2e` (all specs) was not re-run after the Batch 1 fixes + chart fixes — run it first thing next session.
## COMPLETION FORMULA (user decision 2026-09-18): raw = Phase 1 30 + Phase 2 15×(done tasks)/35 + …; percentage = raw ÷ 0.95. Now: 30 + 15×21/35 = 39.0 → 41 % (review-confirmed 41 %). Never bump the previous figure.
## 2026-09-18 01:3x — Final E2E at 572f943: build PASS; E2E 11/11 passed; flaky: e2e/chart.spec.ts:160 (操作エッジの多い課題（b-007）) failed once on TimeoutError waiting for getByTestId("mode-assemble"), passed on Playwright auto-retry #1
