You are implementing Plan 2A Batch E = Task 12 (`src/judge-inspect.ts`, C1/C2 judgment) of `docs/superpowers/plans/2026-09-14-phase2a-fault-content-and-judge.md` in `C:\Users\oltot\Documents\git-projects\OJT` (pnpm monorepo; TypeScript strict + `exactOptionalPropertyTypes` + `noUncheckedIndexedAccess`; Vitest 90 % thresholds; ESLint flat config incl. import-x/no-cycle; Prettier width 100; `.js` import suffixes; `/* c8 ignore next -- reason */`). Read verbatim first: `## Task 12` (L5198–5821), `## 前提` (L15–49), the file table (L50–80), `## 仕様からの意図的な差分` (L8240–8261), and the `## 2B への引き渡し` rows that mention `judgeInspectParts` / `judgeInspectRepair` / `JudgeResult.mode` (L8262–8323). Review edits already folded into the plan text and REQUIRED: I-2 (white-wire rule: in `judgeInspectRepair`, trainee-added wires must be 白; blue additions are colour violations), I-3 (`JudgeResult.mode: 'assemble'` on the existing mode-B result so all three results carry `mode`), M-5 c8 comments. Landed inputs: Tasks 1–11 and 13 (`src/tester.ts` in circuit-sim; content `schema/*`, `faults.ts`, `rng.ts`, `random-faults.ts`, `forbidden.ts`, `highlight.ts`, `inspect-parts.ts`, `inspect-repair.ts`, `reference.ts` with `ReferenceCircuit.cells` and `SchematicProblem`) — read the real signatures (git log for SHAs) and say where the plan text disagrees.
EXTRA REQUIRED FIX (Phase 1 acceptance note, must be in this batch): `packages/content/src/judge.ts:176` (mode-B judge) merges `sessionHazards` with the hazards of the judge replay, so a standing short circuit is reported twice. Change it so the reported hazard list/count for display comes from the session hazards only (the replay's hazards may still gate the verdict if the plan/spec says so — read the surrounding code and §5.6/§14 of the spec `docs/superpowers/specs/2026-09-13-ojt-electrical-trainer-design.md`), add a regression test (session with one `short-circuit-power-on` hazard → judged hazard count 1, not 2), and apply the same rule in the new C1/C2 judges. Note the change in your report and in the plan's 改訂履歴 (one row, docs commit separate).
Files you may touch: `packages/content/src/judge-inspect.ts`, `packages/content/src/judge.ts`, `packages/content/test/judge-inspect.test.ts`, `packages/content/test/judge.test.ts`, `packages/content/test/helpers/inspect.ts` (only if the plan says so), plus the plan doc for the 改訂履歴 row. `src/index.ts` stays untouched (Task 17). Other agents are editing `apps/desktop/**`, `packages/circuit-sim/src/tester.ts` and the built-in JSON files under `packages/content/builtin/` concurrently — never touch those; never `git add -A`/`git add .`; stage exact paths; `git commit --only -- <paths>`; retry on `index.lock`. TDD per step. Verification scoped to the package (`pnpm --filter @ojt/content test`, typecheck, `pnpm exec eslint <files>`, `pnpm exec prettier --write <files>`, `pnpm --filter @ojt/content test -- --coverage`; no custom coverage dirs). Report (≤ 20 lines): SHAs, test counts (plan expected vs actual), deviations, the judge.ts change, and any API mismatch with Tasks 9/10.
