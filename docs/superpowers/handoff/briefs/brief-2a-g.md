You are implementing Plan 2A Task 17 (public API / barrel + whole-repo verification) of `docs/superpowers/plans/2026-09-14-phase2a-fault-content-and-judge.md` in `C:\Users\oltot\Documents\git-projects\OJT` (pnpm monorepo; TypeScript strict; Vitest 90 %; ESLint flat config incl. import-x/no-cycle; Prettier width 100). Read verbatim first: `## Task 17` (L7710–8168), `## 2B への引き渡し` (L8262–8323 — every name in that table must be exported with the stated signature), `## 完了条件` (L8324–8339). Landed: Tasks 1–16 (git log). Step 3's desktop change was applied early in 2272c07 with an inline `p.mode === 'assemble'` predicate and a comment — replace it with the `isAssembleProblem` import once the barrel exports it, and switch `apps/desktop/src/main/content-loader.ts` / `test/content-loader.test.ts` to whatever the plan's Step 3 now says (keep the `unsupported-mode` rows per M-10). Also fold in these deferred review items (from the A+B review): export `SupportedProblem`, `isAssembleProblem`, `isInspectPartsProblem`, `isInspectRepairProblem`, `BUILTIN_ALL_PROBLEMS`; add tests: `isRandomFaults()` true/false (`schema/faults.ts` line ~207 is untested), `ofMode(parseBuiltinProblems([inspectPartsProblemJson()]), isAssembleProblem, 'モードB課題')` throws `BuiltinProblemError`, `mergeProblemSets` with a user C2 overriding a built-in C2 and a user C1 appended; and in `schema/index.ts` (~L138–142, ~L177–182) make a typo'd `mode` string (e.g. `'inspect-part'`) yield ONE issue naming the valid modes instead of 8 assemble-schema issues (test it). Files: `packages/content/src/index.ts`, `packages/content/src/schema/index.ts`, `packages/content/test/index.test.ts`, `packages/content/test/schema-index.test.ts`, `packages/content/test/schema-faults.test.ts`, `packages/content/test/loader.test.ts` or `builtin.test.ts`, `apps/desktop/src/main/content-loader.ts`, `apps/desktop/test/content-loader.test.ts`. Never `git add -A`; stage exact paths; `git commit --only -- <paths>`; retry on `index.lock`. Verification is WHOLE-REPO for this task: `pnpm -r test` (root count), `pnpm typecheck`, `pnpm lint`, `pnpm exec prettier --check .`, `pnpm --filter @ojt/content test -- --coverage` and `pnpm --filter @ojt/circuit-sim test -- --coverage` (thresholds), `pnpm --filter @ojt/content schema:write` (no diff expected in `schema/task.schema.json` except formatting), `pnpm --filter @ojt/desktop build`. If another agent's in-flight desktop work breaks typecheck, report it with the file and stop there rather than fixing their code. Report (≤ 20 lines): SHAs, root test totals per package, coverage, any handoff-table name that could not be exported and why.
