You are implementing Plan 2A Batch D2 = Tasks 9 and 10 of `docs/superpowers/plans/2026-09-14-phase2a-fault-content-and-judge.md` in `C:\Users\oltot\Documents\git-projects\OJT` (pnpm monorepo; TypeScript strict + `exactOptionalPropertyTypes` + `noUncheckedIndexedAccess`; Vitest with 90 % thresholds; ESLint flat config incl. import-x/no-cycle; Prettier width 100; zod 4; `.js` import suffixes; `/* c8 ignore next -- reason */` convention). Read the plan sections verbatim first: Task 9 = lines 3647–4264 (`src/inspect-parts.ts`, C1 check-socket circuit and the judgment table), Task 10 = lines 4265–4629 (`src/inspect-repair.ts`, C2 initial board / repair / modification; `ReferenceCircuit.cells` added in `src/reference.ts` Step 1; `buildInspectRepairCircuit(problem, board, options)` with `options.resolvedFaults?: readonly FaultSpecData[]` at ~L4532 — this option is REQUIRED, it lets the desktop restore a work file without re-running `resolveFaults`). Also read `## 前提` (L15–49), the file table (L50–80) and `## 仕様からの意図的な差分` (L8240–8261) so the deviations are honoured. Implement the plan's code exactly; adapt only names that differ in the real files and list every adaptation in your report.
Already landed (do not redo): Tasks 1–8 (52fe00c 78eaff9 1586cdd 735330e 7b6ee19 936fd96 + Batch C commits for `src/faults.ts` / `src/rng.ts` / `src/random-faults.ts` — read the real signatures there rather than trusting the plan when they disagree, and say so), Task 11 `src/forbidden.ts` (bc0afd0) and Task 13 `src/highlight.ts` (46e79c4). `src/index.ts` stays untouched (Task 17 re-exports).
Two follow-ups owed by Task 10 (Batch D1 left markers): (1) `packages/content/test/highlight.test.ts` builds cells via `assignToBoard(...)` with a comment "switch to `built.value.cells` once Task 10 lands" — do that switch; (2) `packages/content/test/forbidden.test.ts` defines a local `REPAIR_WIRE_COLOR` with a comment pointing at Task 10 — replace it with `import { REPAIR_WIRE_COLOR } from '../src/inspect-repair.js';`. Keep both test files green.
Files you may touch: `packages/content/src/inspect-parts.ts`, `packages/content/src/inspect-repair.ts`, `packages/content/src/reference.ts`, `packages/content/test/inspect-parts.test.ts`, `packages/content/test/inspect-repair.test.ts`, `packages/content/test/reference.test.ts` (if the plan adds a case), `packages/content/test/helpers/inspect.ts` (only if the plan says so), plus the two test files above. Other agents are editing `packages/circuit-sim/src/tester.ts` and `apps/desktop/**` concurrently — never touch those, never run `git add -A`/`git add .`; stage exact paths and commit with `git commit --only -- <paths>`; retry on `index.lock`. One commit per task, messages from the plan.
TDD per step (write test → run RED → implement → GREEN). Verification scoped to the package: `pnpm --filter @ojt/content test`, `pnpm --filter @ojt/content typecheck` (or `pnpm exec tsc -p packages/content --noEmit`), `pnpm exec eslint <your files>`, `pnpm exec prettier --write <your files>`, then `pnpm --filter @ojt/content test -- --coverage` (if another Vitest process holds the coverage lock, retry once after it finishes; never write coverage into a custom directory inside the repo, and delete any `coverage-*` temp dirs you create). Report (≤ 25 lines): per task the SHA, test file counts (plan's expected count vs actual), deviations/adaptations, and anything in Batch C's real API that differed from the plan text.
Batch C facts (landed): Task 4 = commit 4fc3e59 (`src/faults.ts`: `applyFaults` validates against a cloned draft and commits only on zero errors, `injectPartFaults`, `withoutPartFaults`, `matchesSite`, `faultParam`, `reportKindOf`; types `FaultSite`/`AppliedFaults`/`ApplyFaultsResult`/`FaultReport`/`FaultReportKind`). Task 7 = its files were swept into commit 6ae1a3d (`src/rng.ts`: `mulberry32`, `pickIndex`, `pickOne`, `hashSeed`; `src/random-faults.ts`: `resolveFaults`, `MAX_RANDOM_FAULT_ATTEMPTS`, `RANDOM_FAULT_KINDS`, `ResolveFaultsOptions` (with `maxAttempts`), `ResolveFaultsResult`). IMPORTANT: Batch C ALREADY added `export type SchematicProblem = AssembleProblem | InspectRepairProblem` to `src/reference.ts` and widened `toRoles`/`toExtraParts`/`cellPath`/`toProblemPath`/`buildReferenceSession` to accept it — if Task 10 Step 1 tells you to add the same type/widening, reconcile instead of duplicating (keep one definition; add only `ReferenceCircuit.cells` and whatever else is missing). Current package baseline: 25 files / 292 tests, all green; typecheck/lint/prettier clean repo-wide.
