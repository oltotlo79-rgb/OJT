You are implementing Plan 2A Tasks 14 and 16 of `docs/superpowers/plans/2026-09-14-phase2a-fault-content-and-judge.md` in `C:\Users\oltot\Documents\git-projects\OJT` (pnpm monorepo; TypeScript strict + `exactOptionalPropertyTypes` + `noUncheckedIndexedAccess`; Vitest 90 % thresholds; ESLint flat config; Prettier width 100 — `packages/content/builtin/*.json` ARE formatted by Prettier; JSON imports use `with { type: 'json' }`). Read verbatim first: `## Task 14` (L5989–6415: four built-in C1 problems + registration in `builtin/index.ts` incl. `BUILTIN_ALL_PROBLEMS`, `parseBuiltinProblems`/`ofMode` already exist from Task 8 (B-3), the `builtin-discrimination.test.ts` widening (B-4 landed in 936fd96 — check what remains), the B-6 per-problem `it()` split and `testTimeout`), `## Task 16` (L6988–7709: built-in C2 problems 5–8 (1級 form) and the repair 弁別 tests with B-6 timeouts), `## 前提`, `## 仕様からの意図的な差分` (L8240–8261), spec §17. Landed inputs: everything through Task 13 plus Task 12 `src/judge-inspect.ts` (SHA in git log — read the real `judgeInspectParts`/`judgeInspectRepair` signatures and result types; note `JudgeResult.mode`) and Task 15's four `builtin/c2-*.json` + its test (SHA in git log). Write the JSON files exactly as the plan gives them; if a plan JSON fails its own test, fix it minimally, keep the intended fault, and list the fix. Files you may touch: the four `packages/content/builtin/c1-*.json`, the four remaining `builtin/c2-*.json`, `packages/content/builtin/index.ts`, the plan's Task 14/16 test files, `packages/content/test/builtin-discrimination.test.ts`, `packages/content/vitest.config.ts` (only for the B-6 `testTimeout` step if the plan puts it there), `packages/content/test/index.test.ts` only if the plan's Task 14 says so. Do NOT touch `src/index.ts` (Task 17). Other agents may be editing `apps/desktop/**` and `packages/circuit-sim/**` — never touch those; never `git add -A`/`git add .`; stage exact paths; `git commit --only -- <paths>`; retry on `index.lock`. One commit per task with the plan's message + the attribution lines your environment specifies. TDD per step. Verification scoped to the package (`pnpm --filter @ojt/content test --no-file-parallelism`, typecheck, eslint on your files, `pnpm exec prettier --write` on your files, `pnpm --filter @ojt/content test -- --coverage`; no custom coverage dirs; report the wall time of the 弁別 tests). Report (≤ 20 lines): SHAs, problem ids/titles/fault kinds, test counts (plan expected vs actual), JSON fixes, coverage.
Task 15 facts (landed 32025e0): the four files are `packages/content/src/builtin/inspect-repair/c2-001-self-hold.json`, `c2-002-self-hold-contact.json`, `c2-003-on-delay.json`, `c2-004-one-shot.json` (plan JSON verbatim; all wire/terminal ids verified against `toSession()`), test `packages/content/test/builtin-inspect-repair.test.ts` (9 tests) — it parses the JSON files directly via `parseProblem()`/`isInspectRepairProblem` and uses a per-`it()` 180 s timeout because `builtin/index.ts` and `vitest.config.ts` were out of its scope. Task 14 must therefore (a) register the C2 problems in `builtin/index.ts` exactly as the plan says (`BUILTIN_INSPECT_REPAIR_PROBLEMS`, `BUILTIN_ALL_PROBLEMS`, …), (b) apply the plan's Step 5a `vitest.config.ts` `testTimeout` change, and (c) switch `test/builtin-inspect-repair.test.ts` to import from `../src/builtin/index.js` (drop the per-`it()` override if the global timeout covers it). Task 16 adds the remaining four C2 problems (1級 form) to the same folder and extends the same test file or adds the plan's second file — follow the plan's names.
