# このプロジェクトでの作業規則

- `OJT` のルートは、Gitの共通管理ディレクトリ（通常は `OJT/.git`）の親とする。worktreeの現在地を基準に外側へ作業フォルダを作らない。
- 作業場所・成果物・調査資料は、この `OJT` フォルダ内に収める。親の `git-projects` に `OJT-codex-*` や `OJT-wt-*` などの作業フォルダを新設しない。
- 未保存の変更を保護するため worktree が必要な場合は、既存の `OJT/.worktrees/phase7` を優先する。作業ごとに worktree を増やさない。
- 作業用フォルダは、変更の反映と未保存ファイルの内容を確認して不要になった時点で片付ける。削除を拒否された場合は回避せず、残った場所と理由を報告する。
- 配布物は `OJT/release/<version>`、検証記録は `OJT/release/verification` に置く。ポータブル版のお渡し用フォルダにはEXEを1個だけ置く。
- コミットコメントは変更内容が分かる日本語にする。既存のGit名義・認証アカウントを勝手に変更しない。
- サブエージェントは使用しない。
- 元の作業ツリーにある未保存変更や `docs/reviews/` の資料を上書き・削除しない。
