import type { JSX } from 'react';

/**
 * 結果画面。設計仕様 §12.1。
 *
 * このファイルは Task 6（文言・色・外枠と画面遷移）が `routes.tsx` の全ルートを配線するために
 * 最小限だけ先出ししたもの。判定結果の各部品は Task 15（結果画面）でこのファイルに実装する。
 */
export function Result(): JSX.Element {
  return <div />;
}
